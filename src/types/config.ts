export type TConfig = {
    titlebar: boolean,
    theme: string,
    auto_update: string,
    tray: boolean,
    popup_search: boolean,
    stay_on_top: boolean,
    default_origin: string,
    origin: string,
    ua_window: string,
    ua_tray: string,
    port: number,
    global_shortcut?: string,
}